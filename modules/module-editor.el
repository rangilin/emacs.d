;; Use 2 space to indent by default.
(setq-default indent-tabs-mode nil)
(setq tab-width 2)



;; Make tab indent then complete.
(setq-default tab-always-indent 'complete)



(provide 'module-editor)
